<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.empsys.backend.mapper.PermissionMapper">

    <!-- 根据用户ID获取权限信息 -->
    <select id="getPermissionByUserId" resultType="com.empsys.backend.entity.Permission">
        SELECT * FROM permission
        WHERE user_id = #{userId}
    </select>
    
    <!-- 检查用户是否有编辑权限 -->
    <select id="checkEditPermission" resultType="java.lang.Boolean">
        SELECT can_edit FROM permission
        WHERE user_id = #{userId}
    </select>
    
    <!-- 检查用户是否有删除权限 -->
    <select id="checkDeletePermission" resultType="java.lang.Boolean">
        SELECT can_delete FROM permission
        WHERE user_id = #{userId}
    </select>
    
    <!-- 更新用户权限 -->
    <update id="updatePermission">
        UPDATE permission
        SET can_edit = #{canEdit}, 
            can_delete = #{canDelete}
        WHERE user_id = #{userId}
    </update>
    
    <!-- 插入用户权限 -->
    <insert id="insertPermission">
        INSERT INTO permission (permission_id, user_id, can_edit, can_delete)
        VALUES (#{permissionId}, #{userId}, #{canEdit}, #{canDelete})
    </insert>
    
</mapper> 
