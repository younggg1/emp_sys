<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.empsys.backend.mapper.SystemSettingsMapper">

    <!-- 根据学校ID获取系统设置 -->
    <select id="getSettingsBySchool" resultType="com.empsys.backend.entity.SystemSettings">
        SELECT * FROM system_settings
        WHERE school_id = #{schoolId}
    </select>
    
    <!-- 获取是否需要验证码设置 -->
    <select id="isRequireCaptcha" resultType="java.lang.Boolean">
        SELECT require_captcha FROM system_settings
        WHERE school_id = #{schoolId}
    </select>
    
    <!-- 获取是否需要审批设置 -->
    <select id="isRequireApproval" resultType="java.lang.Boolean">
        SELECT require_approval FROM system_settings
        WHERE school_id = #{schoolId}
    </select>
    
    <!-- 更新系统设置 -->
    <update id="updateSettings">
        UPDATE system_settings
        SET require_captcha = #{requireCaptcha},
            require_approval = #{requireApproval}
        WHERE school_id = #{schoolId}
    </update>
    
    <!-- 插入系统设置 -->
    <insert id="insertSettings">
        INSERT INTO system_settings (setting_id, school_id, require_captcha, require_approval)
        VALUES (#{settingId}, #{schoolId}, #{requireCaptcha}, #{requireApproval})
    </insert>
    
</mapper> 
