<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.empsys.backend.mapper.CounselorMapper">

    <!-- 根据辅导员ID获取辅导员信息 -->
    <select id="getCounselorById" resultType="com.empsys.backend.entity.Counselor">
        SELECT * FROM counselor
        WHERE counselor_id = #{counselorId}
    </select>
    
    <!-- 根据学校ID获取辅导员列表 -->
    <select id="getCounselorsBySchool" resultType="com.empsys.backend.entity.Counselor">
        SELECT * FROM counselor
        WHERE school_id = #{schoolId}
        ORDER BY counselor_id
    </select>
    
    <!-- 获取辅导员管理的班级列表 -->
    <select id="getCounselorClasses" resultType="java.lang.String">
        SELECT DISTINCT class_name FROM student
        WHERE counselor_id = #{counselorId}
        ORDER BY class_name
    </select>
    
    <!-- 获取辅导员管理的学生数量 -->
    <select id="countStudents" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM student
        WHERE counselor_id = #{counselorId}
    </select>
    
    <!-- 添加辅导员 -->
    <insert id="insertCounselor">
        INSERT INTO counselor (counselor_id, school_id, name, department)
        VALUES (#{counselorId}, #{schoolId}, #{name}, #{department})
    </insert>
    
    <!-- 更新辅导员信息 -->
    <update id="updateCounselor">
        UPDATE counselor
        SET name = #{name},
            department = #{department}
        WHERE counselor_id = #{counselorId}
    </update>
    
</mapper> 
