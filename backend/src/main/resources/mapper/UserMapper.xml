<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.empsys.backend.mapper.UserMapper">

    <!-- 根据用户名和学校ID查询用户 -->
    <select id="getUserByUsername" resultType="com.empsys.backend.entity.User">
        SELECT * FROM user
        WHERE username = #{username} AND school_id = #{schoolId}
    </select>
    
    <!-- 查询管理员用户 -->
    <select id="getAdminBySchool" resultType="com.empsys.backend.entity.User">
        SELECT * FROM user
        WHERE school_id = #{schoolId} AND role = 'admin'
        LIMIT 1
    </select>
    
    <!-- 获取学校所有用户列表 -->
    <select id="getUsersBySchool" resultType="com.empsys.backend.entity.User">
        SELECT * FROM user
        WHERE school_id = #{schoolId}
        <if test="role != null">
            AND role = #{role}
        </if>
        ORDER BY user_id
    </select>
    
    <!-- 检查用户密码 -->
    <select id="checkPassword" resultType="java.lang.Boolean">
        SELECT COUNT(*) > 0 FROM user
        WHERE user_id = #{userId} AND password = #{password}
    </select>
    
    <!-- 删除用户(级联删除相关数据) -->
    <delete id="deleteUser">
        DELETE FROM user WHERE user_id = #{userId}
    </delete>
    
    <!-- 更新用户角色 -->
    <update id="updateUserRole">
        UPDATE user
        SET role = #{role}
        WHERE user_id = #{userId}
    </update>
    
</mapper> 
